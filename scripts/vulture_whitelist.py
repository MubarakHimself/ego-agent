"""Vulture whitelist — only justified false positives.

Add names here when Vulture cannot see dynamic use (CLI entry, pytest
fixtures referenced by name, etc.). Prefer fixing dead code over growing
this list.
"""

# intentionally empty for first ship — grow only with justification
