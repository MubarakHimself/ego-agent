# ego-agent

Personal multi-agent CDP browser pool (research → build).

**Not** a fork of CitroLabs ego lite. Inspired by public MIT harness patterns and peer CDP stacks.

## Status

Architecture locked — see sibling notes repo `MubarakHimself/ego-agent-notes` (`ARCHITECTURE.md`, `reports/ego-arch-pool-001.md`).

MVP: Browser Pool Manager, K=5 Chromium slots, lease/heartbeat/release, Space=`user-data-dir`, Linux-first.

## Constraints

- No Electron / Chromium fork / TypeSafe Jev / Open-Jev
- Laya optional later (decision head only)
